# Changelog

## 0.5.0 - 2026-08-02

- 新增最近 72 小时本地累计已知集合，将多次检查见过的帖子 ID 求并集。
- 弹窗分开显示“本次读取”和“72 小时累计已知”，避免把单次 X 网页结果误称为权威总数。
- 累计集合精确按发布时间滚动清理，帖子超过 72 小时后自动移除。
- 普通帖子累计缓存不保存正文，只保存 ID、时间和本地分类结果。
- 新增按日期诊断：本次数量、累计数量、相关数量、滚动次数及完成状态。
- 规则设置变化时自动重建累计分类缓存，避免新旧规则混算。
- 新增跨检查并集、自动过期和按日期汇总测试。

## 0.4.0 - 2026-08-02

- 自动识别 Microsoft Edge 与 Google Chrome，移除通知和界面中写死的 Edge 名称。
- 根据浏览器界面语言自动显示简体中文或英文，无需用户手动选择。
- 本地化弹窗、设置页、帖子分类说明、时间文本、系统通知、登录提示和常见检查错误。
- 为不同浏览器显示对应的后台运行建议和浏览器标识。
- 使用 Chrome 扩展 `_locales` 机制本地化 Manifest 名称与说明。
- 新增浏览器识别、语言选择和英文通知测试。

## 0.3.1 - 2026-08-02

- 修复后台 X 页面仍在缓慢加载时被固定看门狗过早判定为 `scan_timeout` 的问题。
- 扫描进度会刷新看门狗；完全无进度时自动刷新当前页面重试一次。
- 单个日期区间超时后继续后续日期，并在最后尝试“帖子与回复”页，不再直接中止整次补查。
- 为每个页面阶段增加独立标识，忽略刷新或切换页面后迟到的旧扫描消息。
- 弹窗分别显示全部帖子、额度相关帖子和重置帖子数量，并明确普通帖子不会进入事件历史或触发通知。
- 新增本地匹配统计与跨页面去重测试。

## 0.3.0 - 2026-08-02

- 将 72 小时补查按 UTC 日历日期拆分为多个宽泛的作者查询，通常依次检查 4 个日期区间。
- 每次滚动后自适应等待 X 动态加载，减少因为加载较慢而过早判断“没有更多结果”。
- 跨搜索页按帖子 ID 汇总去重，弹窗显示不同帖子总数和已检查日期区间数。
- 只有所有日期区间都耗尽当前可见结果才显示“分段补查完成”；网页端完整性仍不冒充 X API 级完整。
- 任一日期区间失败或触发安全上限时，继续使用 Tibo“帖子与回复”页作为本地回退来源。
- 新增日期分段与查询范围自动测试。

## 0.2.1 - 2026-08-02

- 当最新搜索结果过早停止时，自动回退到 Tibo“帖子与回复”页继续 72 小时补查。
- 将“历史覆盖有限”和真正的连续检查失败分开显示。
- 修复重复点击“测试通知”时 Edge 只更新旧通知而不再次弹出的问题。

## 0.2.0 - 2026-08-02

- 调整为 Microsoft Edge 优先、Chrome 兼容定位。
- 改用不带严格关键词的 `from:thsottiaux` 最新搜索页。
- 新增 72 小时时间范围补查、无限滚动和完整性报告。
- 新增启动、安装/更新、弹窗打开、手动检查和断档后的补查触发。
- 持久化成功/尝试时间、去重 ID、最近帖子、错误、状态和补查摘要。
- 新增实时与延迟发现通知分级，超过 72 小时只记历史。
- 新增可能重置、已经重置、额度/规则变化和否定表达规则。
- 新增 X 登录失效、连续失败和补查不完整提示。
- 历史限制为 50 条；去重 ID 保留 14 天并限制为 3,000 个。
- 移除不必要的 `tabs` 权限。
- 重做 Edge 风格弹窗和通知图标。
- 增加完整测试手册、GitHub / Edge 商店检查清单和商店文案草稿。

## 0.1.0 - 2026-08-02

- 首个 Manifest V3 原型。
- 通过置顶 X 页面监测 `@thsottiaux` 的帖子与回复。
- 支持定时刷新、动态 DOM 观察、帖子去重和本地历史。
- 支持即将重置、已经重置、一般额度消息和可储存重置分类。
- 提供 Chrome / Edge 安装说明、隐私说明和自动化测试。
