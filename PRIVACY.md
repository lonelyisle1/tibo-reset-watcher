# Tibo Reset Watcher 隐私说明

最后更新：2026-08-02

Tibo Reset Watcher 是一个本地运行、单一用途的浏览器扩展：监测 `@thsottiaux` 的公开 X 帖子，并提醒 ChatGPT、Codex、ChatGPT Work 额度重置、额度增加或规则变化。

## 用户账号

- 用户无需创建或登录本扩展账号。
- 用户需要在安装扩展的同一个 Edge 或 Chrome 配置文件中保持 X.com 登录，以便 X 搜索页显示公开帖子。
- 扩展不会要求用户输入 X 用户名或密码。

## 本机处理和保存的数据

扩展只在 `chrome.storage.local` 中保存完成监测所需的数据：

- 设置：启用状态、检查间隔、补充额度词语；
- `lastSuccessfulCheckAt`：最近一次成功读取 X 内容的时间；
- `lastAttemptAt`：最近一次尝试检查的时间；
- `processedPostIds`：已处理的帖子 ID；
- 去重元数据：帖子发布时间和本机处理时间，用于清理过旧 ID；
- `lastSeenPostId`：最近看到的帖子 ID；
- `lastError` 和 `monitoringStatus`：错误和运行状态；
- 最近补查范围、是否完整、读取数量和结束原因；
- 最近 72 小时累计见过的公开帖子 ID、发布时间、首次/最近看到时间和本地分类结果；普通帖子不在该缓存中保存正文；
- 最近 50 条命中规则的额度相关事件，包括公开帖子文本、ID、发布时间、链接、帖子类型、发布账号、发现时间和本地分类结果。

去重 ID 默认保留 14 天并限制数量；额度事件历史最多 50 条。

## 明确不读取、收集、保存或上传的数据

扩展不会有意读取、收集、保存或上传：

- X 密码；
- Cookie、会话令牌或认证标头；
- X 私信；
- 用户个人资料；
- 浏览历史；
- ChatGPT 账号、聊天内容、提示词或实际额度；
- 联系人、位置、支付信息、设备标识或广告标识；
- 与监测 Tibo 公开额度消息无关的数据。

扩展没有开发者服务器、分析统计、广告、遥测、跨设备同步或远程代码。代码不会把本地数据发送给扩展开发者。

## 读取的公开信息

内容脚本仅在 X 搜索页面运行，并只接受发布账号路径为 `@thsottiaux` 的公开帖子。处理字段包括：

- 帖子文本；
- 帖子 ID；
- 发布时间；
- 帖子链接；
- 帖子类型；
- 发布账号。

扩展使用帖子 ID 去重。即使同一公开帖子在搜索结果、回复或其他页面形态中重复出现，也只处理一次。

## 权限用途

| 权限 | 用途 |
| --- | --- |
| `storage` | 本地保存设置、运行状态、去重 ID 和最近 50 条额度事件。 |
| `alarms` | 定时唤醒 MV3 service worker、执行检查、补查重试和超时保护。 |
| `notifications` | 显示额度事件、X 登录失效和连续检查失败通知。 |
| `https://x.com/*` | 创建、刷新并读取 X 搜索页中已显示的 Tibo 公开帖子。 |

扩展不申请 `tabs`、`cookies`、`history`、`downloads`、`<all_urls>` 或与功能无关的权限。

## 第三方服务

用户访问 X.com 时，页面和网络请求由 X 按其自身条款与隐私政策处理，与用户正常访问 X 页面相同。本扩展不代表、控制或代理 X，也不会绕过登录、验证码、访问限制或速率限制。

## 用户控制

用户可以：

- 在弹窗中暂停监测；
- 在设置中清空最近额度事件历史；
- 关闭扩展创建的监测页，但下一次启用检查时可能重新创建；
- 在 `edge://extensions/` 或 `chrome://extensions/` 停用或卸载扩展；
- 通过卸载扩展删除全部扩展本地数据。

“清空历史”会保留去重 ID，以避免旧帖重新触发通知。

## 数据传输与安全

本版本没有开发者控制的网络端点，不传输本地存储数据。X 页面自身与 X 服务器之间的通信由浏览器和 X 控制。

## 未来变更

若未来版本增加服务器、API、分析统计、同步、远程模型或新的数据用途，必须在发布前更新本说明、README、权限和商店披露。不会在未披露的情况下扩展数据用途。

## 联系方式

在 GitHub 发布后，隐私问题和删除请求应通过项目仓库的 Issues 或仓库中列出的维护者联系方式提交。发布者应在提交 Edge Add-ons 前补充正式支持链接，并把本隐私说明托管到公开 HTTPS URL。
